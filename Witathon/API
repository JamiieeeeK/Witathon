#python -m pip install google-genai
from google import genai
def chat(prompt):
    client = genai.Client(api_key="AIzaSyBc4J3g7KDhJUnW4zasbQyR2blE6xsnOtU")
    response = client.models.generate_content(
        model="gemini-3-flash-preview",
        contents= prompt + "reply simply in maximum 3 lines, and just reply to my answer, without other text, stay short and meaningful"
    ) #i am trying to make the prompt correct
    return response.text

prompt = ""
while prompt != "-1":
    prompt = input("Enter:")
    print(chat(prompt))
